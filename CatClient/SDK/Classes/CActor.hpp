#pragma once

class CActor {
public:
	//Vector3 getPosition() { /* not tested since i dont have ClientState to actually get the LocalPlayer */
	//	float x = *reinterpret_cast<float*>((uint64_t)this + 0x4C4);
	//	float y = *reinterpret_cast<float*>((uint64_t)this + 0x4C8);
	//	float z = *reinterpret_cast<float*>((uint64_t)this + 0x4CC);

	//	return Vector3(x, y, z);
	//}
};